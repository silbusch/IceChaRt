% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReadSIGRID3.R
\name{read_sigrid3}
\alias{read_sigrid3}
\title{Describe a sea-ice polygon based on SIGRID3 code}
\usage{
read_sigrid3(
  shp,
  polygon_id,
  id_col = "ID_NEW",
  save_txt = TRUE,
  out_path = NULL,
  out_dir = NULL
)
}
\arguments{
\item{shp}{A \code{terra::SpatVector} loaded from a CIS ice chart shapefile.}

\item{polygon_id}{A single ID or vector of IDs identifying the target polygon(s).}

\item{id_col}{Name of the column holding polygon IDs (default: \code{"ID_NEW"}).}

\item{save_txt}{Logical. If \code{TRUE}, output is saved as a .txt file.}

\item{out_path}{Optional full file path for the output \code{.txt} file. If \code{NULL},
the file is saved automatically to \code{IceChaRt_output/SIGRID3_text/} with a timestamp.}

\item{out_dir}{Optional base directory for automatic output. If \code{NULL}, the current
working directory is used. Ignored when \code{out_path} is provided.}
}
\value{
Invisibly returns the description string; output is printed via \code{cat()}.
}
\description{
Looks up one polygon by ID from a shapefile in SIGRID3-code and returns a
description of its ice conditions, including concentration,
stage of development, and ice form for each polygon.
}
\details{
Non-ice polygons (Land, Water, No Data, Ice Shelf) are reported with a
short one-line message instead.
}
\examples{
\dontrun{
path <- system.file("extdata", "cis_SGRDREA_20201102T.gpkg", package = "IceChaRt")
ice_chart <- terra::vect(path)

# Example with just one polygon and prinitng the text only in the console
read_sigrid3(ice_chart, polygon_id=3, save_txt = FALSE)

# List of polygons and saving the text in a .txt file
read_sigrid3(ice_chart, polygon_id=c(3, 19, 77, 108), save_txt = TRUE)
}
}
